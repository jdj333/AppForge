# Core dependencies
openai>=1.40.0

# Optional but recommended
python-dotenv>=1.0.1   # for loading OPENAI_API_KEY from .env
rich>=13.7.0           # prettier console output (tables, colors)

# Compatibility helpers
httpx>=0.27.0          # optional, for direct HTTP calls if needed
